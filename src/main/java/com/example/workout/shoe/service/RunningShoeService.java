package com.example.workout.shoe.service; import com.example.workout.shoe.model.RunningShoe; import org.springframework.data.domain.*; import java.util.Set; public interface RunningShoeService { RunningShoe create(RunningShoe shoe); RunningShoe get(Long id); Page<RunningShoe> search(Pageable pageable); RunningShoe patch(Long id, RunningShoe patch, Set<String> fields); void delete(Long id); }
